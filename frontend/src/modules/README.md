# Frontend modules

Keep each feature's pages, components, API client, state, hooks, and tests in its owner's module folder. Use `src/components` only for genuinely shared UI.

- `auth` - Maira
- `workspaces` - Khadija
- `editor` - Muzammil
- `collaboration` - Namra
- `comments` - Ayyan
- `history-search` - Aiman
- `files-dashboard` - Shanza
