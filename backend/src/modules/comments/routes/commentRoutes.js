import { Router } from 'express';

export const commentRouter = Router();

// TODO: register routes once controller is implemented
// commentRouter.post('/', ...);
// commentRouter.get('/document/:documentId', ...);
// commentRouter.post('/:id/reply', ...);
// commentRouter.patch('/:id/resolve', ...);
// commentRouter.delete('/:id', ...);
